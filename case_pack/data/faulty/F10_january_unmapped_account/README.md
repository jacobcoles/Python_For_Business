# Variant `F10` - reporting period 2026-01

The input set for 2026-01 as received, with 2025-12 supplied so the period-on-period
movement check can run.

One control will fail. Document what it is, decide whether the pack can be
released, and only then look in `late_delivery/` for what the business sent
you afterwards. Preserve the raw exports exactly as received.

Run your controls before you believe any total. `source_controls.csv` and
`expected_files.csv` come from the units' own records and are **not** derived from the
transaction files in this folder. That is what makes them worth checking against.
